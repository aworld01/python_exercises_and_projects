"""
WHAT IS MODULE?
==============
A module is a file containing Python definitions and statements. That means, module containing python functions, classes and variables.

WHAT IS THE USE OF MODULE?
=========================
Reusability


TYPES OF MODULES
===============
1: Default Modules
2: Third Party Modules

pip install xlwt #to install Third Party Modules

pip list #to see the list of installed module (works in commandLine)

help("modules") #to see the list of installed module

help(math) #to see the documentation of math module (need to import first)

print(dir(math)) #to see the functions and variables in math module (need to import first)

print(len(dir(pt))) #to check number of of modules (need to import first)
"""
"""example1"""
# help("modules") #to see the installed module

"""example2"""
import csv
# print(dir(csv))

"""example3"""
import csv
help(csv)